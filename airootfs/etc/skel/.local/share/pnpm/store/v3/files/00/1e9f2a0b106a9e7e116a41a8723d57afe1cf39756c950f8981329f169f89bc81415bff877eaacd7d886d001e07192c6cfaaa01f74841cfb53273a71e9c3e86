/// <reference types="react" />
import { OptionType } from "../../utils/types";
import { User } from "discord-types/general";
declare const _default: {
    name: string;
    description: string;
    authors: {
        name: string;
        id: bigint;
    }[];
    settings: import("../../utils/types").DefinedSettings<{
        enableIsStaff: {
            description: string;
            type: OptionType.BOOLEAN;
            default: false;
            restartNeeded: true;
        };
    }, import("../../utils/types").SettingsChecks<{
        enableIsStaff: {
            description: string;
            type: OptionType.BOOLEAN;
            default: false;
            restartNeeded: true;
        };
    }>, {}>;
    patches: ({
        find: string;
        replacement: {
            match: RegExp;
            replace: string;
        };
        predicate?: undefined;
    } | {
        find: string;
        predicate: () => boolean;
        replacement: ({
            match: RegExp;
            replace: (_: string, user: string, flags: string) => string;
        } | {
            match: RegExp;
            replace: string;
        })[];
    } | {
        find: string;
        replacement: {
            match: string;
            replace: string;
        };
        predicate?: undefined;
    })[];
    isStaff(user: User, flags: any): boolean;
    settingsAboutComponent: () => JSX.Element;
    WarningCard: import("react").FunctionComponent<any>;
} & Record<string, any>;
export default _default;
